using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Bullet : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        //set dir and speed
        int dir = 0;
        int speed = 1;

    }

    // Update is called once per frame
    void Update()
    {
        //go forward


        //listen for combatant rigidbody
        
            
    }
}
