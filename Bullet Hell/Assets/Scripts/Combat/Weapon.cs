using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Weapon : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Weapon_speed = 1;
    }

    // Update is called once per frame
    void Update()
    {
        //on mouse click


        //spawn bullet, set dir, set speed


    }
}
